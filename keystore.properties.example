# Copy this file to `keystore.properties` and fill with real values for release signing.
# Do not commit `keystore.properties`.
storeFile=release-keystore.jks
storePassword=change-me
keyAlias=release
keyPassword=change-me
